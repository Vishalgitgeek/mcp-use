# MCP Integration Service - Production Environment Variables
# Copy this to /opt/mcp-service/.env and fill in your values

# ===========================================
# REQUIRED - Must be set before deployment
# ===========================================

# Composio API Key (from https://app.composio.dev)
COMPOSIO_API_KEY=your_composio_api_key

# Agent API Key (generate a secure random string)
# Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
AGENT_API_KEY=your_secure_agent_api_key

# Google OAuth (from Google Cloud Console)
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# JWT Secret (generate a secure random string)
# Use: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=your_jwt_secret_here

# ===========================================
# DOMAIN CONFIGURATION - Update with your domain
# ===========================================

# Your domain (MUST match DNS and SSL certificate)
OAUTH_REDIRECT_BASE=https://mcp.yourdomain.com

# ===========================================
# DATABASE - Choose one option
# ===========================================

# Option 1: Local MongoDB (on same EC2)
MONGODB_URI=mongodb://localhost:27017

# Option 2: MongoDB Atlas (recommended for production)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority

MONGODB_DB_NAME=mcp_integrations

# ===========================================
# SERVER CONFIGURATION - Usually don't change
# ===========================================

MCP_SERVICE_HOST=0.0.0.0
MCP_SERVICE_PORT=8001
